# scx-rusty-sync
Automatically change the state of the scx_rusty CPU scheduler when the power supply changes.

Install
-

```bash
git clone https://github.com/sonbontuoi/scx-rusty-sync.git
cd ./scx-rusty-sync
chmod +x ./install.sh
sudo ./install.sh
```